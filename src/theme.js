import {
  createMuiTheme,
} from '@material-ui/core/styles';

// create a  MUITheme

export default createMuiTheme({
  palette: {
    primary: {
      main: '#e53935',
    },
    secondary: {
      main: '#3f51b5',
    },
  },
});
